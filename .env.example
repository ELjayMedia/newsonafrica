# Supabase project URL used by both browser and server runtimes.
# The Data API base URL is derived as: ${NEXT_PUBLIC_SUPABASE_URL}/rest/v1
NEXT_PUBLIC_SUPABASE_URL=https://anhjovxdgwepobsgudya.supabase.co

# Public anonymous key for browser/client-side reads and authenticated user flows.
# Safe to expose in bundled client code when RLS policies are configured correctly.
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Server-only service role key for privileged backend operations.
# Never expose this value to the browser or commit real values to version control.
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# WordPress GraphQL endpoints for each site. Use a full URL ending in /graphql.
# Local: point to local/staging WordPress instances; Preview: use deploy-specific preview endpoints; Production: use live site endpoints.
NEXT_PUBLIC_WP_SZ_GRAPHQL=https://sz.example.com/graphql
NEXT_PUBLIC_WP_ZA_GRAPHQL=https://za.example.com/graphql
NEXT_PUBLIC_WP_NG_GRAPHQL=https://ng.example.com/graphql

# Default site code used when no site is selected (for example: sz, za, or ng).
# Keep this aligned per environment (local/preview/production) with the primary site for that deployment.
NEXT_PUBLIC_DEFAULT_SITE=sz

# Public base URL for this frontend application (no trailing slash preferred).
# Local: http://localhost:3000, Preview: your preview deployment URL, Production: canonical public domain.
NEXT_PUBLIC_SITE_URL=https://news.example.com
